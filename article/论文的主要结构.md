论文的主要结构

必须严格按照docx文件(article\南方科技大学本科生毕业设计（论文）郭祖源-Mark.docx)的内容安排来（docx文件是以前一个学长写的，不过它是工程专业的 我是物理系的需要补充一些基础的理论例如不同石墨烯堆垛的性质以及为什么要找到它）

下面是我对于这个项目的整体的写的想法，这些主要是正文的部分不包括其他部分的写法，你参考这些想法然后严格按照docx文件的内容安排来写一篇初稿。

segmentation:

论文中的模型架构介绍；

直接复用论文中模型的效果，展示可视化图（config: configs\M2F\config_baseline.yaml, 可视化在training_log\eval_vis\training_log\eval_vis\baseline_raw_no_postprocess 目前这里面数据有点问题因为包含了后处理所以请你目前不要用这里面的数据来分析后续我会把它完全还原成原版模型的训练效果）；

我的改进：拉高模型的判断阈值（从前100个最大可能变为前20个最大可能的flake 进入损失函数计算 + 增大模型对小块，碎块的惩罚力度（对于反复击中的样本，只在第一次击中记录为击中；后面的击中都算作无效，然后在损失函数中增加未击中次数这个惩罚项）+ 对于模型的输出进行后处理，若相互包含则合并，颜色相同的块如果离得很近进行连接，去除面积在100平方微米以下的块，去除周长/面积过大的块（config: configs\M2F\base_config.yaml, 可视化test集：training_log\eval_vis\modified_config）；

（你可以根据我的模型，我没提到的重要细节请你加上，并写出为什么这样修改有效）

效果：可视化图接近人工手动标注，基本能识别肉眼可以识别出来的flake, 展示改进后相同图片的识别效果（区别明显）；

green channel:

论文中的方法介绍（具体可参见 green channel.pdf）；

问题：仅1-3层且由于设备以及条件不同完全无法套用它们的数据；

我的改进：baseline: 使用线性回归+增加白平衡参数的影响，对于每个块做工程化的处理（按flake的bbox进行外扩，背景和flake采用不同的双边滤波方式，并且对背景进行回归，增大了数值稳定性）；(代码在layer_recognition\baselines.py，数据在 training_log\wls_baseline_largest_inner_wb1_v10 )

然后由于效果并不能达到预期，因此继续改进 改进的代码在 layer_recognition\green_ordinal.py

根据代码总结回归的方法和选用的特征，写出数学公式，并给出详细的理由。

改进后效果：0-7层的识别准确率为84%， 0-9层识别准确率也达到了77.6% ±1层的识别率达到接近100%；（由于人的标注其实也存在误差我们认为这个方法对于数据的区分度非常不错，你这里可以讲每一层的平均分数，已经拉开了很大的差距）
